# 有道领世高考喜报素材库：2026 上线交接说明

本文档用于在新对话中继续推进 2026 素材导入、GitHub 推送、Cloudflare 资源接入、PostHog 数据埋点统计。当前项目目录：

```text
D:\销售转化策略\三年-高考喜报
```

当前页面是原生静态前端：

```text
index.html
styles.css
app.js
data.js
scripts/scan-materials.js
package.json
```

当前已有年份：

```text
2024/
2025/
```

当前 `data.js` 已由扫描脚本生成，页面支持：

- 年份筛选：全部 / 2025 / 2024，后续扫描 2026 后会自动出现。
- 省份筛选：统一下拉框，按拼音排序。
- 学科筛选：按钮枚举。
- 六大模块：顶尖名校、超高分喜报、单科高分、高分学员、提分学员、押题学员反馈。
- 查看全部弹窗：每页 15 张，5 列 × 3 行。
- 图片灯箱预览。
- 复制图片本体，失败时降级复制图片链接。
- PostHog 初始化代码已存在，但 token / host 仍是占位符。

## 一、2026 素材导入前的目录要求

请把 2026 素材整理成以下结构：

```text
2026/
├─ 2026_单科高分/
├─ 2026_押题学员反馈/
├─ 2026_提分学员/
├─ 2026_超高分喜报/
├─ 2026_顶尖名校/
└─ 2026_高分学员/
```

推荐文件命名：

```text
2026_学科_省份_分数_学生名.jpg
2026_学科_省份_学科94，总分645.jpg
2026_省份_总分_学生名.jpg
2026_省份_学校_学生名.jpg
2026_学科_省份_提分描述.jpg
2026_学科_省份_1.jpg
```

注意：

- 省份必须作为独立下划线字段，例如 `2026_安徽_上海交通大学_何昂宸.jpg`，不要依赖学校名里的“上海”判断省份。
- 如果图片属于辽宁，就不要再用 `东北四省`。
- 修改图片名、增删图片后，网页不会自动同步，必须重新扫描。

## 二、本地扫描与校验

导入或修改素材后，在项目根目录执行：

```powershell
npm.cmd run scan
```

该命令会重新生成：

```text
data.js
```

本地预览：

```powershell
npm.cmd run dev
```

访问：

```text
http://127.0.0.1:8088/index.html
```

检查清单：

- 年份下拉中出现 `2026`。
- `全部` 年份数量 = 2024 + 2025 + 2026。
- 2026 六个模块数量正确。
- 省份筛选正常。
- 学科筛选正常。
- 查看全部弹窗正常。
- 点击图片能打开灯箱。
- 复制按钮不打开灯箱。
- 控制台无明显 JS 报错。

## 三、GitHub 推送前处理

GitHub 只提交代码和数据索引，不提交源图片、不提交压缩包、不提交密钥。

建议 `.gitignore` 包含：

```text
2024/
2025/
2026/
*.zip
*.7z
node_modules/
.env
.env.*
outputs/
```

推荐提交文件：

```text
index.html
styles.css
app.js
data.js
package.json
scripts/scan-materials.js
.gitignore
Cloudflare-R2-image-library-guide.md
PostHog数据埋点设置流程.md
2026上线交接说明.md
```

推送前检查：

```powershell
git status --short
```

确认不要出现：

```text
2024/
2025/
2026/
2025.zip
node_modules/
.env.r2
```

## 四、Cloudflare R2 资源接入方案

当前第一版 `data.js` 中：

```js
thumbUrl
previewUrl
imageUrl
```

都指向本地图片路径。上线时建议改为 R2 地址。

推荐 R2 bucket 结构：

```text
gaokao-success-gallery/
├─ thumbs/
├─ previews/
└─ originals/
```

三类图片用途：

- `thumbUrl`：首页和弹窗卡片缩略图。
- `previewUrl`：灯箱预览图。
- `imageUrl`：复制、下载使用的高清原图。

建议下一步让 Codex 新增 R2 构建脚本：

```text
scripts/build-r2-assets.js
scripts/upload-r2-assets.js
```

目标流程：

```powershell
npm.cmd run build:r2-assets
npm.cmd run upload:r2-assets
```

构建脚本应做的事：

- 扫描 2024 / 2025 / 2026 源图片。
- 为每张图生成 thumb / preview。
- 原图进入 originals。
- 生成 upload manifest。
- 生成替换为 R2 URL 的 `data.js`。

上传脚本应做的事：

- 读取 `.env.r2`。
- 上传 thumbs / previews / originals。
- 支持跳过已存在文件。
- 不把密钥写进代码。

本地 `.env.r2` 示例：

```text
CLOUDFLARE_ACCOUNT_ID=
R2_BUCKET=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_PUBLIC_BASE_URL=
```

同时创建 `.env.r2.example`，只放字段名，不放真实值。

## 五、Cloudflare R2 CORS

如果要在网页中复制图片本体，R2 必须允许前端 fetch 图片。

R2 bucket CORS 示例：

```json
[
  {
    "AllowedOrigins": [
      "https://你的-pages项目.pages.dev",
      "http://127.0.0.1:8088",
      "http://localhost:8088"
    ],
    "AllowedMethods": ["GET", "HEAD"],
    "AllowedHeaders": ["*"],
    "ExposeHeaders": ["Content-Length", "Content-Type", "ETag"],
    "MaxAgeSeconds": 86400
  }
]
```

上线后把本地 origin 视情况移除。

## 六、Cloudflare Pages 部署

如果使用 Cloudflare Pages 托管静态网页，建议：

```text
Framework preset: None
Build command: 留空 或 exit 0
Build output directory: /
Production branch: main
```

如果图片已走 R2，则 Pages 只负责托管：

```text
HTML / CSS / JS / data.js
```

不要把源图片放 Pages 或 GitHub。

## 七、PostHog 数据埋点

注意：工具名称是 PostHog，不是 posthug。

当前 `index.html` 已有 PostHog 初始化占位符：

```text
POSTHOG_PROJECT_TOKEN_PLACEHOLDER
POSTHOG_API_HOST_PLACEHOLDER
```

上线前替换为：

```text
Project token
API Host
```

常见 API Host：

```text
https://us.i.posthog.com
https://eu.i.posthog.com
```

以 PostHog 后台显示为准。

当前 `app.js` 已有统一埋点函数：

```js
trackEvent(eventName, properties = {})
```

已覆盖事件：

```text
tool_page_viewed
filter_changed
province_selected
subject_selected
filter_search_clicked
reset_clicked
image_clicked
image_copy_clicked
image_copy_succeeded
image_copy_failed
section_page_changed
section_view_all_clicked
```

因为当前页面已删除顶部搜索框，`filter_search_clicked` 暂时不会由搜索框触发，但函数和事件设计仍保留。

建议在 PostHog Dashboard 建以下图表：

- 页面访问量：`tool_page_viewed`
- 省份筛选热度：`filter_changed`，筛选 `filter_name = province`
- 年份筛选热度：`filter_changed`，筛选 `filter_name = year`
- 学科筛选热度：`filter_changed`，筛选 `filter_name = subject`
- 图片点击排行：`image_clicked`，按 `image_name` breakdown
- 图片复制点击排行：`image_copy_clicked`，按 `image_name` breakdown
- 图片复制成功量：`image_copy_succeeded`
- 图片复制失败量：`image_copy_failed`
- 模块查看全部点击：`section_view_all_clicked`

## 八、新对话推荐提示词

可以在新对话中直接发送：

```text
请继续当前项目：
D:\销售转化策略\三年-高考喜报

我已经准备导入 2026 高考喜报素材。请先阅读：
1. 2026上线交接说明.md
2. Cloudflare-R2-image-library-guide.md
3. PostHog数据埋点设置流程.md

目标：
1. 检查 2026 素材目录结构和命名是否能被 scripts/scan-materials.js 正确解析。
2. 运行 npm.cmd run scan，确认 data.js 包含 2024 / 2025 / 2026。
3. 本地预览页面，验证年份筛选、六模块数量、查看全部、灯箱、复制按钮。
4. 准备 GitHub 提交，但不要提交源图片、zip、node_modules、密钥。
5. 设计并实现 Cloudflare R2 资源构建与上传流程，让 thumbUrl / previewUrl / imageUrl 指向 R2。
6. 替换 PostHog token 和 api_host 占位符，并验证事件上报。

注意：
- 不要重构核心页面逻辑。
- 不要删除现有 2024 / 2025 数据。
- 不要把密钥写入代码或提交 GitHub。
- 不要一次性把源图片提交到 GitHub。
```

## 九、当前项目重要提醒

- 修改图片名或增删图片后，必须运行：

```powershell
npm.cmd run scan
```

- 刷新页面仍旧显示老数据时，使用强制刷新：

```text
Ctrl + F5
```

- 年份筛选已有 `全部`，后续 2026 扫描进 `data.js` 后会自动出现在下拉框。
- 省份未知项显示为 `未知省份`。
- 三栏当前比例为：

```css
grid-template-columns: 3fr 4fr 2fr;
```

- 查看全部弹窗当前为每页 15 张，不是加载更多。

